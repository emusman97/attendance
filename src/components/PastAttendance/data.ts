import { AppStrings } from '../../constants';
import type { SelectItems } from '../../types';

export const AttributeItems: SelectItems = [
  { title: AppStrings.Date, value: 'date' },
  { title: AppStrings.Status, value: 'status' },
];
